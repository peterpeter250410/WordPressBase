diff --git a/wp-content/themes/wpbase-starter/functions.php b/wp-content/themes/wpbase-starter/functions.php
index 26aa1e3..c9ee894 100644
--- a/wp-content/themes/wpbase-starter/functions.php
+++ b/wp-content/themes/wpbase-starter/functions.php
@@ -1165,11 +1165,13 @@ add_filter('rest_url_prefix', function() {
     return 'api';  // 改成你想要的路径，比如 'api' 或 'json-api'
 });
 
-add_action('init', function() {
-    if (defined('REST_REQUEST') && REST_REQUEST) {
-        error_log('REST API 请求进来啦！路径: ' . $_SERVER['REQUEST_URI']);
-    }
-});
+/* ─── SEO ───
+   canonical / hreflang / title / description / OG / robots.txt
+   JSON-LD 结构化数据 / 多语言 sitemap
+   均适配 /zh/ /en/ 语言前缀，三语共用同一套模板 */
+require_once get_template_directory() . '/inc/seo-core.php';
+require_once get_template_directory() . '/inc/seo-schema.php';
+require_once get_template_directory() . '/inc/seo-sitemap.php';
 
 /* ─── Flush Rewrite Rules on Activation ─── */
 function eikou_rewrite_flush() {
